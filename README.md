Helper scripts to setup Claymore miner on Ubuntu 16.04

Emsure that screen is installed on your system, if missing, package can be downloaded as listed below.
`sudo apt install screen`

Ensure that ENV_VARS are updated to match those of every new machine and user.
